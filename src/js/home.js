import _ from 'lodash';

module.exports = (db, cols) => {
  return {
    data() {
      return {
      }
    },

    firestore: {
    },

    methods: {
    },

    template: '#template-page-home'
  };
};
