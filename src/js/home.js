import _ from 'lodash';

module.exports = (cols) => {
  return {
    data() {
      return {
      }
    },

    firestore: {
    },

    methods: {
    },

    template: '#template-page-home'
  };
};
