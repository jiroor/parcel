import _ from 'lodash';

module.exports = (db, cols) => {
  return {
    data() {
      return {
      }
    },

    firestore() {
      return {
      }
    },

    computed: {
    },

    methods: {
    },

    template: '#template-page-event__complete'
  };
};
